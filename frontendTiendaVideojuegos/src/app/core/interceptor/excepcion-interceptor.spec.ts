import { ExcepcionInterceptor } from './excepcion-interceptor';

describe('ExcepcionInterceptor', () => {
  it('should create an instance', () => {
    expect(new ExcepcionInterceptor()).toBeTruthy();
  });
});
